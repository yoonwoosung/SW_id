# 트러블슈팅 (PAAR 템플릿)

- 제목:
- 관련 Issue:
- 관련 PR:
- 관련 ADR:
- 발생 환경:
- 재현 조건:

## Problem — 문제가 무엇이었는가

무엇이, 어떤 상황에서, 어떻게 잘못됐는지 작성한다.

## Analysis — 어떻게 원인을 분석했는가

로그·재현·이력 추적 등 원인을 좁혀간 과정을 작성한다. 검토했지만 아니었던 가설도 적으면 좋다.

## Action — 어떤 해결 방법을 적용했고 왜 선택했는가

- 적용한 해결:
- 검토한 대안:
- 선택 이유:

## Result — 어떻게 검증했고 무엇이 달라졌는가

- 검증 방법(테스트/재현):
- 개선 전후 수치: (측정된 값만. 없으면 "정성적 개선"으로 명시)
- 남은 한계:

---

> 예시(형식 참고용): "회원정보 수정 제출 시 500" 문제를 로그의 `IntegrityError: NOT NULL constraint failed: user.nickname`로 원인을 특정하고, 폼에 없는 필드를 None으로 덮어쓰던 코드를 방어하도록 수정한 뒤 회귀 테스트로 검증. — 실제 사례로 채울 때는 본인 PR 링크와 검증 로그를 첨부한다.
